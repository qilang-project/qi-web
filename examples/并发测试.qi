包 主程序;

导入 标准库.时间;
导入 Web::{
    应用, 上下文, 响应,
    创建应用, 配置, 获取, 文本, 运行应用
};

函数 慢处理(上下文值: 上下文) : 响应 {
    时间.睡眠毫秒(500);
    返回 文本("done");
}

函数 入口() {
    变量 应用值: 应用 = 创建应用();
    应用值 = 配置(应用值, "127.0.0.1", 6759);
    应用值 = 获取(应用值, "/slow", 慢处理);
    运行应用(应用值);
}
