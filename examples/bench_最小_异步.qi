包 主程序;

// 真 M:N 版本：tokio runtime + 异步 accept + per-conn tokio 任务
// 只有 num_cpus 个 OS 线程，但能撑 1000+ 并发连接

导入 Web::{应用, 上下文, 响应, 创建应用, 配置, 获取, 文本, 运行应用_异步};

函数 处理(上下文值: 上下文) : 响应 {
    返回 文本("ok");
}

函数 入口() {
    变量 应用值: 应用 = 创建应用();
    应用值 = 配置(应用值, "127.0.0.1", 6794);
    应用值 = 获取(应用值, "/", 处理);
    运行应用_异步(应用值);
}
